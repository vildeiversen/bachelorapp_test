package no.oslomet.travelbehavior.ui.theme

import androidx.compose.ui.graphics.Color

// Brand greens
val PrimaryGreen = Color(0xFFA8D5BA)      // light, good as container/tint
val SecondaryGreen = Color(0xFF7FBF9D)    // mid green
val DarkGreen = Color(0xFF4E8E67)         // dark green, good as primary (CTA)

// Neutrals
val BackgroundLight = Color(0xFFF8FAF9)
val SurfaceLight = Color(0xFFFFFFFF)
val CardSecondaryBackground = Color(0xFFE9EFEA)

val TextDark = Color(0xFF2C2C2C)
val TextLight = Color(0xFFFFFFFF)

// Accents
val AccentBlue = Color(0xFF5A90C8)

// Use as semantic "error" (destructive actions)
val ErrorRed = Color(0xFFE57373)

// Material-style containers (helps avoid using pastel as primary button)
val PrimaryContainerGreen = PrimaryGreen
val SecondaryContainerGreen = Color(0xFFD7EFE1) // soft green tint, slightly lighter than PrimaryGreen
