package no.oslomet.travelbehavior.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Brand greens: Primary colors used to represent the app's visual identity.
 */
val PrimaryGreen = Color(0xFFA8D5BA)      // Light green, for containers and background tints.
val SecondaryGreen = Color(0xFF7FBF9D)    // Mid-tone green for secondary UI elements.
val DarkGreen = Color(0xFF4E8E67)         // Deep green, for primary call-to-action buttons.

/**
 * Neutrals: Core colors for general app surfaces and backgrounds.
 */
val BackgroundLight = Color(0xFFF8FAF9)   // Main app background.
val SurfaceLight = Color(0xFFFFFFFF)      // Background for surfaces like cards and sheets.
val CardSecondaryBackground = Color(0xFFE9EFEA) // Secondary surface color for subtle contrast.

/**
 * Typography: Colors optimized for text readability.
 */
val TextDark = Color(0xFF2C2C2C)           // Primary text color for light backgrounds.
val TextLight = Color(0xFFFFFFFF)          // Text color for dark backgrounds or primary buttons.

/**
 * Accents: Colors for highlights.
 */
val AccentBlue = Color(0xFF5A90C8)         // General accent color for highlights.

/**
 * Semantics: Colors for specific statuses like errors.
 */
val ErrorRed = Color(0xFFE57373)           // Used for errors and warning messages.

/**
 * Material-style containers: Variants used to maintain theme.
 */
val PrimaryContainerGreen = PrimaryGreen
val SecondaryContainerGreen = Color(0xFFD7EFE1) // Soft green tint for lighter container backgrounds.
